// Design tokens shared across DreamAgent sections.
// Font-family strings are factored out because they recur on nearly every
// element; colors stay as inline literals in components to keep a 1:1 match
// with the source design.

/** Display / headline typeface — used on every heading regardless of language. */
export const DISPLAY = "'Bricolage Grotesque', Arial, Helvetica, sans-serif";

/** Monospace — labels, eyebrows, UI chrome, buttons. */
export const MONO = "'IBM Plex Mono', monospace";
